# This is just a dummy settings.py as a workaround to allow 
# the Django helper modules to load. Annoying, but required if
# we want to use the easy datetime -> json serialization provided
# by DjangoJSONEncoder
